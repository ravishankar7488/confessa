<!-- Navbar (sticky on top) -->
<nav class="navbar navbar-expand-lg navbar-dark bg-dark sticky-top py-2">
  <div class="container-fluid">
    <!-- Top row: Brand + Search + Toggler -->
    <div class="d-flex align-items-center w-100 flex-nowrap">
      <!-- Brand -->
      <a class="navbar-brand me-2 flex-shrink-0" href="/">Confessa</a>

      <!-- Search form (always visible) -->
      <form
        class="d-flex flex-grow-1 mx-2 search-form"
        action="/search"
        method="POST"
        style="min-width: 0"
      >
        <input
          class="form-control form-control-sm me-2 search-input"
          type="search"
          name="query"
          placeholder="Search..."
          aria-label="Search"
          required
        />
        <button
          class="btn btn-outline-light btn-sm flex-shrink-0"
          type="submit"
        >
          Search
        </button>
      </form>

      <!-- Toggler -->
      <button
        class="navbar-toggler ms-1 flex-shrink-0"
        type="button"
        data-bs-toggle="collapse"
        data-bs-target="#navbarNav"
        aria-controls="navbarNav"
        aria-expanded="false"
        aria-label="Toggle navigation"
      >
        <span class="navbar-toggler-icon"></span>
      </button>
    </div>

    <!-- Collapsible links -->
    <div class="collapse navbar-collapse mt-2 mt-lg-0" id="navbarNav">
      <!-- Left links -->
      <ul class="navbar-nav me-auto">
        <li class="nav-item">
          <a class="nav-link" href="/">All Confessions</a>
        </li>
        <li class="nav-item">
          <a class="nav-link" href="/create">Create ➕</a>
        </li>
        <% if (user === "lucifer") { %>
        <li class="nav-item">
          <a class="nav-link" href="/admin/newconfessions">New Confessions</a>
        </li>
        <% } %>
      </ul>

      <!-- Right links -->
      <ul class="navbar-nav ms-auto">
        <% if (user !== "lucifer") { %>
        <li class="nav-item">
          <a class="nav-link" href="/adminlogin">Admin Login</a>
        </li>
        <% } else { %>
        <li class="nav-item">
          <a class="nav-link" href="/logout">Logout</a>
        </li>
        <% } %>
      </ul>
    </div>
  </div>
</nav>

<!-- Custom styles -->
<style>
  .navbar {
    min-height: 70px;
  }

  /* Search input width on desktop */
  @media (min-width: 992px) {
    .search-input {
      max-width: 250px; /* Adjust this width as you like */
    }
  }

  /* Adjust search box on mobile */
  @media (max-width: 576px) {
    .search-form {
      margin: 0 5px;
    }
    .search-form input {
      min-width: 0;
    }
  }

  /* Prevent buttons from wrapping to next line */
  .navbar .d-flex.flex-nowrap {
    flex-wrap: nowrap !important;
  }

  @media (min-width: 992px) {
    .navbar .collapse.navbar-collapse {
      display: flex !important;
      justify-content: space-between;
      align-items: center;
    }
  }
</style>
